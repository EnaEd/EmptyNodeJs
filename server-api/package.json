{
  "main": "dist/app",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "start": "node --inspect -r dotenv/config dist/app.js",
    "build:launch": "tsc && npm run start",
    "watch": "nodemon --watch src --ignore 'src/**/*.spec.ts' -e ts --exec npm run build:launch"
  },
  "prettier": {
    "semi": true,
    "trailingComma": "all",
    "singleQuote": true,
    "printWidth": 70
  },
  "nodemon": {
    "watch": [
      "src"
    ],
    "ext": "ts",
    "ignore": [
      "src/**/*.spec.ts"
    ],
    "exec": "tsc && node --inspect dist/app.js"
  },
  "dependencies": {
    "core-util-is": "^1.0.2",
    "dotenv": "^8.2.0",
    "koa": "^2.11.0",
    "koa-router": "8.0.8"
  },
  "devDependencies": {
    "@types/koa": "2.0.45",
    "@types/koa-router": "^7.0.28",
    "@types/node": "^13.7.1",
    "nodemon": "^2.0.2",
    "tslint": "^6.0.0",
    "typescript": "^3.7.5"
  }
}
